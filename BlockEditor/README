Original code by Roberto Bucher, 12.04.2014, roberto.bucher@supsi.ch
Heavily modified to allow for myhdl netlisting

contents:
block.py            contains block class and block import/export
connection.py       contains all code to draw connections
dialg.py            contains all dialog routines
editor.py           contains diagram editor (scene) code, used in pyEdit.py
libary.py           import/export of all libraries
netlist.py          generate myhdl netlist (also works as stand-alone script)
port.py             draw in/output pins and nodes
pyEdit.py           mainwindow for diagram editor
src_import.py       dynamic python module import from an arbitrary filename
svg_utils.py        svg support (used for icon edit with inkscape)
template_files.py   templates for netlisting/block-creation etc.
